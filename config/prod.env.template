# RHOAI Ops Buddy - Production Configuration Template
# Copy this to config/prod.env and fill in your values

# =============================================================================
# Jenkins MCP Server
# =============================================================================
JENKINS_URL=https://your-jenkins-server.example.com
JENKINS_USER=your-jenkins-username
JENKINS_PASSWORD=your-jenkins-password

# =============================================================================
# Cluster Monitor MCP Server
# =============================================================================
HIVE_KUBECONFIG_HOST=/home/your-user/.kube/hive.yaml
HIVE_CONTEXT=hive-cluster

# =============================================================================
# LLM Configuration
# =============================================================================
LLM_BASE_URL=https://your-llm-endpoint
LLM_API_KEY=your-llm-api-key
LLM_MODEL=your-llm-model
LLM_TEMPERATURE=0.7

# =============================================================================
# LangSmith (Required - used by chat-ui API passthrough)
# =============================================================================
LANGSMITH_API_KEY=lsv2_your_api_key

# =============================================================================
# Chat UI Build-Time Variables (for building production images)
# =============================================================================
# NEXT_PUBLIC_API_URL: The URL browsers use to reach the API proxy
# - Use /api (relative) for portable images that work on any domain
# - Use full URL (e.g., https://ops-buddy.apps.cluster.example.com/api) for specific deployments
NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_ASSISTANT_ID=agent

# =============================================================================
# LangGraph API URL (runtime - for chat-ui API passthrough)
# =============================================================================
# Where chat-ui backend proxies requests to langgraph-server
# Default: http://langgraph-server:8000 (internal podman network)
# Override if chat-ui cannot reach langgraph via internal network:
# LANGGRAPH_API_URL=https://ops-buddy-api-rhoai-ops.apps.your-cluster.example.com
